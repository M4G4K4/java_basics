package org.example.basics.record;

// This PersonRecord is the same as the Person class , this type of class
// already by default implements all the methods in Person
public record PersonRecord(String name, int personId){}
